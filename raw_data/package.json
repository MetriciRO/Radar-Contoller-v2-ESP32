{
  "name": "radar-interface",
  "version": "1.0.0",
  "description": "",
  "default": "index.html",
  "scripts": {
    "clean:dist": "rimraf dist",
    "clean:build": "rimraf build",
    "start": "npm run clean:dist && parcel index.html --dist-dir ./dist",
    "build": "npm run clean:build && parcel build index.html --dist-dir ./build --no-source-maps --no-cache"
  },
  "author": "Sloata George",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.3.2",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@parcel/compressor-gzip": "^2.3.2",
    "core-js": "^3.21.1",
    "regenerator-runtime": "^0.13.9",
    "toastify-js": "^1.11.2"
  }
}