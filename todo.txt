Split cpp code into headers;

Write radar data into a JSON:
 - config.json

Create API that sends radar data:
 - https://mocki.io/v1/4f35e17d-f292-4e32-bf8c-d837fb16de1e

Use Parcel bundler to create interface;

Link the bundled interface to ESP32 back-end;

Move .javascript/build to ./data

Create Modal for Network and Radar Settings forms (same as reset):
    - Empty inputs will not change
    - Maybe replace empty inputs with "WILL NOT CHANGE" or the current value

POST Data Lifecycle:
 - Post data from interface to server;
 - On server:
    * updateState(jsonDoc): read POSTed data and update live state
    * writeStateToFile(): write live state to /config.json


 - Open /config.json
 - Create JSON Document from /config.json
 - Replace key[value] in JSON Document
 - Write JSON Document in /config.json


Implement Logs

Implement Toast